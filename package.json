{
  "name": "type-safe-shortcuts",
  "version": "0.0.1",
  "description": "Type Safe Electron Shortcuts",
  "main": "./dist/main/main.js",
  "types": "./dist/main/main.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest --config jestconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm run test"
  },
  "files": [
    "/dist/main"
  ],
  "author": "mlrv",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.19",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "typescript": "^4.1.3"
  },
  "peerDependencies": {
    "electron": "^11.1.1"
  }
}
